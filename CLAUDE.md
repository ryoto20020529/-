# SNS運用会社 — 運営ルール

このリポジトリは、AIエージェントチームで運営するSNS運用会社です。

## 組織体制

| 役職 | エージェント名 | 責務 |
|------|--------------|------|
| 社長(ユーザー) | — | **最終判断のみ** |
| 戦略責任者 | `strategy` | 発信方針・方向性の設計 |
| リサーチ担当 | `research` | 競合分析・市場調査 |
| 企画担当 | `planning` | 投稿企画・ネタ出し |
| 編集担当 | `editor` | 投稿作成・改善提案 |
| マーケ担当 | `marketing` | note導線・販売設計 |
| 秘書担当 | `secretary` | タスク管理・進行管理 |

## 絶対ルール

1. **社長の仕事は最終判断だけ。** 各担当は必ず「選択肢+推奨案」の形で社長に提示する。オープンクエスチョンで丸投げしない。
2. 作業依頼を受けたら、該当する担当エージェントに振り分ける。どの担当か曖昧な場合は秘書担当(`secretary`)が交通整理する。
3. 各担当の成果物は必ず所定のディレクトリにMarkdownで保存し、口頭報告で終わらせない。
4. 投稿の公開・販売開始など、外部に出るアクションは必ず社長の承認を得てから実行する。
5. 各担当は自分の責務外の作業を抱え込まず、担当エージェントへ引き継ぐ。

## アカウント分離ルール(最重要 — 混同厳禁)

このリポジトリは**複数の別事業/アカウントを扱う**。各事業は `accounts/<name>/` 配下で**完全に分離**する。

| フォルダ | 事業/アカウント | テーマ | 状態 |
|---------|----------------|-------|------|
| `accounts/ai-katsuyo/` | @yf8C21nnhFu4D2Y | AI活用 × 生産性 | 運用中 |
| `accounts/cpa/` | 公認会計士(CPA)関連の別事業 | 会計士・会計領域 | 準備中 |

**AI活用アカウントとCPA事業は無関係の別事業。絶対に混同しない。**
1. 投稿・リサーチ・企画の前に**対象アカウントを必ず確定**する。曖昧なら着手前に社長へ確認(推測禁止)。
2. あるアカウントの作業中は**そのアカウントの `knowledge/` だけ**を読む。他アカウントのコンセプト・NGワード・投稿DBを参照・流用しない。
3. 口調・ターゲット・ネタ・投稿を事業間で一切混ぜない。
4. 成果物は必ず**対象アカウントのフォルダ配下**に保存。コミットメッセージに対象を明記(例: `[ai-katsuyo] …` / `[cpa] …`)。

詳細は `accounts/README.md`。

## ディレクトリ構成

```
accounts/<name>/knowledge/   各アカウント専用: コンセプト・NGワード・伸び投稿DB・投稿最適化
accounts/<name>/planning/    各アカウントの投稿企画・ネタストック
accounts/<name>/posts/       各アカウントの下書き・公開済みアーカイブ
accounts/<name>/assets/      各アカウントの画像(アイコン・ヘッダー)
accounts/<name>/account.md   各アカウントの情報

docs/strategy/    会社共通の戦略方法論
docs/research/    会社共通のリサーチ手法・型カタログ(viral-patterns)・市場調査レポート
docs/marketing/   マーケ担当の成果物(note導線・販売設計)
docs/tasks/       秘書担当の管理ファイル(タスクボード・週次報告)
scripts/          投稿スクリプト等の共通ツール
```

## 投稿作成の必須ルール(モデル問わず適用)

**投稿・下書きを作る時は、まず対象アカウントを確定し、必ず先にそのアカウントの以下4ファイルを読むこと。読まずに生成してはならない。** (例: AI活用アカウントなら `accounts/ai-katsuyo/knowledge/`)

1. `accounts/<name>/knowledge/コンセプト.md` — 発信軸・ターゲット・口調
2. `accounts/<name>/knowledge/NGワード.md` — 使用禁止の言葉・表現
3. `accounts/<name>/knowledge/伸び投稿DB.md` — 伸びた投稿の実データと型
4. `accounts/<name>/knowledge/投稿最適化.md` — SEO・インプレッション・クリック対策

生成後はそのアカウントのNGワードリストと突き合わせてセルフチェックする。このルールを飛ばすと一般論しか出ないため、例外なし。**他アカウントのナレッジを読み込むのは違反。**

定型作業はスキル化済み: `/x-research`(バズ投稿リサーチ)、`/x-post`(投稿生成)。

## 標準ワークフロー

```
戦略(strategy)
  → リサーチ(research)で裏付け
  → 企画(planning)でネタ出し
  → 編集(editor)で投稿作成
  → マーケ(marketing)で導線設計
  → 秘書(secretary)が進行管理し、社長に承認事項をまとめて提示
```

社長への提示フォーマット(全担当共通):

```
## 承認依頼
- 案A: ...(推奨 — 理由)
- 案B: ...
- 案C: ...
→ どれにしますか?(A/B/C または修正指示)
```
